# 步骤05：睡眠页主路径优化与最终验证

## 目标

强化睡眠页“今晚开始”的主路径，同时保留模块、定时和最近配置能力。

## 涉及文件

- `src/features/sleepSession/SleepSessionView.tsx`
- `src/features/sleepSession/SleepSessionView.css`
- 相关测试文件

## 待办拆分

- [ ] 睡眠页首屏保留“今晚的会话”和主操作。
- [ ] 会话模块、定时和最近配置纳入更明确的设置区。
- [ ] 保留无配置时去声音页配置的入口。
- [ ] 测试覆盖主按钮禁用、可启动和高级区仍可访问。
- [ ] 使用 Chrome MCP 检查桌面和移动首屏。
- [ ] 运行 `pnpm run check`。
- [ ] 提交本步骤后推送 `origin/main`。

## 实现要点

- 首屏突出当前配置和开始睡眠。
- 会话模块、定时和最近配置作为更明确的设置区域展示。
- 移动端悬浮控制不遮挡主按钮。
- 完成后运行完整质量门禁。
- 不改变睡眠计时器状态机和最近配置存储。

## 验收标准

- 当前配置存在时主按钮可启动睡眠。
- 没有当前配置时主按钮禁用且能跳转声音页配置。
- 会话模块、定时和最近配置仍可访问。
- `pnpm run check` 通过。

## 验证命令

```bash
rtk pnpm exec vitest run src/features/appMode/AppWorkspace.test.tsx --testNamePattern "睡眠|sleep"
rtk pnpm run check
```

Chrome MCP 手动验证：

- 桌面视口打开睡眠页。
- 移动视口 `390x844x2,mobile,touch` 打开睡眠页。
- 确认主按钮不被悬浮控制遮挡。

## 提交边界

提交睡眠页源码、样式和相关测试。最终验证通过后推送所有本轮提交。

## 回滚点

如果折叠或设置区影响现有睡眠流程，回滚本步骤布局改动，保留前四步提交。

## 检查清单

- [ ] 测试覆盖睡眠页主路径。
- [ ] 移动端截图检查无明显遮挡。
- [ ] 文档与最终实现一致。
- [ ] 最终提交后推送到远端。
