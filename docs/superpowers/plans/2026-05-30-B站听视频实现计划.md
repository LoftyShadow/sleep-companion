# B站听视频实现计划

## 目标

新增“听视频”模式，第一版只支持 B 站官方播放器嵌入。用户粘贴 B 站链接后，应用解析 `BV`、`av`、`ep` 或直播房间号，并按来源类型载入对应官方播放器。

本轮补充体验细节：点击“载入”后通过 `autoplay=1` 尝试自动播放，并在链接输入区提供“粘贴”按钮读取系统剪贴板文本。

本轮追加直播支持：普通视频和番剧继续使用 `player.bilibili.com/player.html`；直播间链接使用 `www.bilibili.com/blackboard/live/live-activity-player.html?cid=房间号`。解析结果通过枚举区分视频和直播，再由策略工厂生成统一 iframe source。

本轮追加收听面板外观：右侧主视觉改成类似音乐播放器的当前来源卡片，官方 iframe 缩小为播放源区域。该改动只改变 UI 外观，不抓取真实媒体流，不自建播放控制。

本轮追加外层收听控制：直播播放器通过官方 `postMessage` 支持外层播放/暂停和音量控制；普通视频外链没有公开外部控制接口，暂停通过卸载播放源停止声音，音量仍需在官方播放源内调整。

本轮追加移动端模式导航优化：Android Tauri 端把“声音 / 听书 / 听视频”固定为底部三项导航栏，页面滚动到中段后仍可直接切换模式，并为全面屏手势区域预留底部空间。

本轮追加标题和封面展示：载入普通视频、番剧或直播间后，通过 Tauri 后端命令只读获取 B 站标题和封面元信息，展示在右侧收听面板。元信息获取失败不阻断官方播放器载入，界面回退展示解析出的 B 站标识。

## 执行步骤

- [x] 新增 B 站链接解析工具和单元测试。
- [x] 新增听视频 React 视图，包含链接输入、载入按钮、状态和官方 iframe 播放区。
- [x] 新增听视频样式，保持与现有工作型界面一致。
- [x] 扩展顶层应用模式为“声音 / 听书 / 听视频”。
- [x] 补充集成测试，覆盖模式入口和 B 站链接载入。
- [x] 运行聚焦测试、完整门禁和 diff 检查。
- [x] 调整播放器 URL 为 `autoplay=1`，在用户点击载入后尝试自动播放。
- [x] 新增“粘贴”按钮读取剪贴板文本并填入链接输入框。
- [x] 补充剪贴板粘贴回归测试并运行聚焦测试。
- [x] 扩展 B 站解析模型，新增视频/直播来源枚举和策略工厂。
- [x] 支持 `live.bilibili.com` 直播间链接生成官方直播播放器 URL。
- [x] 更新听视频 UI 文案，让状态和错误提示覆盖直播输入。
- [x] 补充直播解析和集成载入测试。
- [x] 将右侧播放区调整为音频收听面板外观，并保留官方播放器作为播放源。
- [x] 新增外层播放/暂停控制和直播音量控制。
- [x] 在 Android Tauri 端将顶层模式切换固定为底部三项导航栏。
- [x] 为移动端底部导航栏补充安全区和内容避让空间。
- [x] 运行聚焦测试、完整门禁、Android debug 构建和模拟器截图检查。
- [x] 新增受限的 B 站元信息 Tauri 命令，只返回标题和封面。
- [x] 新增前端元信息 loader，并在听视频收听面板展示标题和封面。
- [x] 补充普通视频和直播元信息展示测试。

## 影响文件

- 新增 `src/features/videoListening/bilibiliVideo.ts`
- 新增 `src/features/videoListening/bilibiliVideo.test.ts`
- 新增 `src/features/videoListening/bilibiliMetadata.ts`
- 新增 `src/features/videoListening/bilibiliMetadata.test.ts`
- 新增 `src/features/videoListening/VideoListeningView.tsx`
- 新增 `src/features/videoListening/VideoListeningView.css`
- 新增 `src-tauri/src/bilibili_metadata.rs`
- 修改 `src-tauri/src/lib.rs`
- 修改 `src-tauri/Cargo.toml`
- 修改 `src/features/appMode/appModeTypes.ts`
- 修改 `src/features/appMode/AppModeSwitcher.tsx`
- 修改 `src/features/appMode/AppModeSwitcher.css`
- 修改 `src/features/appMode/AppWorkspace.tsx`
- 修改 `src/features/appMode/AppWorkspace.test.tsx`
- 修改 `src/features/appMode/AppWorkspace.integration.test.tsx`

## 关键约束

- 只使用 B 站官方视频外链播放器和官方直播活动播放器。
- 不抓取真实音视频流，不下载、不缓存、不转码。
- 不隐藏官方播放器后只播放音频。
- 不绕过 B 站登录、会员、地区、版权、广告或其它限制。
- 自动播放只通过官方播放器参数尝试，不做浏览器策略绕过。
- 播放链接解析只做本地字符串处理；标题和封面元信息通过受限 Tauri 命令只读请求 B 站接口，不请求真实音视频流，不联网跟随短链接。
- Tauri 元信息命令只接受已经解析出的 `bvid`、`aid`、`ep` 或直播房间号，不接受任意 URL。
- 听视频模式切换只改变可见性，不主动停止 `PlayerPort` 或 `TtsEnginePort`。
- 文档正文保持中文。

## 验收命令

先运行聚焦测试：

```bash
rtk pnpm vitest run src/features/videoListening src/features/appMode
```

最后运行完整门禁：

```bash
rtk pnpm run check
rtk git diff --check
```

## 剩余风险

- B 站官方播放器对部分视频可能要求登录、会员、地区或版权条件，应用不做绕过。
- 桌面 WebView 对第三方 iframe 的策略可能与浏览器不同，需要按实际平台验证。
- 标题和封面元信息接口可能因为网络、接口策略或内容权限失败；失败时只影响收听面板展示，不影响官方播放器载入。
- `b23.tv` 短链接如果不包含可解析路径，第一版不会联网跟随跳转，因此无法识别。
